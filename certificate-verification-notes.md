# Certificate Update Verification Notes

- The full-page desktop preview renders the portfolio without a build error and shows the redesigned Certifications & Workshops area with image-backed records.
- The section visibly contains the IIRS–ISRO certificate artwork and additional certificate thumbnails/cards, consistent with database-backed rendering.
- The anchor screenshot request rendered the homepage hero rather than auto-scrolling to the anchor, so the exact certificate text/order should be verified through the API/database query and a targeted runtime check before delivery.
